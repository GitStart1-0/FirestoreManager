rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Production rules live in D:/AndroidDevProject/noesis/firestore.rules.
      // Keep this fallback closed to prevent accidental database exposure.
      allow read, write: if false;
    }
  }
}
